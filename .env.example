# Index builder (Python). Both keys are used only by `gospel build` and
# `gospel search` — the embedding pass needs OpenAI, the query pipeline Claude.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
